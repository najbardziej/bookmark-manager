import { ConvertUrlToDomainPipe } from './convert-url-to-domain.pipe';

describe('ConvertUrlToDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertUrlToDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
